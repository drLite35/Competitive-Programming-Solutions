#include <iostream>
#include <string>
using namespace std;

int main() {
    int len;
    cin >> len;
    while(len-->0){
        int a,b;
        cin >> a >> b;
        cout << a+b << endl;
    }
    
    return 0;
}